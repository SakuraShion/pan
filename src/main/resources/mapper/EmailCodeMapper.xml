<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.mywork.mapper.EmailCodeMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.example.mywork.entity.EmailCode">
        <id column="email" property="email" />
        <result column="code" property="code" />
        <result column="create_time" property="createTime" />
        <result column="status" property="status" />
    </resultMap>

    <!--实体映射-->
    <resultMap id="base_result_map" type="com.example.mywork.entity.EmailCode">
        <!--邮箱-->
        <result column="email" property="email"/>
        <!--编号-->
        <result column="code" property="code"/>
        <!--创建时间-->
        <result column="create_time" property="createTime"/>
        <!--0:未使用  1:已使用-->
        <result column="status" property="status"/>
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        email, code, create_time, status
    </sql>

    <sql id="base_condition_filed">
        <if test="query.email != null and query.email!=''">
            and email = #{query.email}
        </if>
        <if test="query.code != null and query.code!=''">
            and code = #{query.code}
        </if>
        <if test="query.createTime != null and query.createTime!=''">
            <![CDATA[ and  create_time=str_to_date(#{query.createTime}, '%Y-%m-%d') ]]>
        </if>
        <if test="query.status != null">
            and status = #{query.status}
        </if>
    </sql>

    <sql id="query_condition">
        <where>
            <include refid="base_condition_filed"/>
            <if test="query.emailFuzzy!= null  and query.emailFuzzy!=''">
                and email like concat('%', #{query.emailFuzzy}, '%')
            </if>
            <if test="query.codeFuzzy!= null  and query.codeFuzzy!=''">
                and code like concat('%', #{query.codeFuzzy}, '%')
            </if>
            <if test="query.createTimeStart!= null and query.createTimeStart!=''">
                <![CDATA[ and  create_time>=str_to_date(#{query.createTimeStart}, '%Y-%m-%d') ]]>
            </if>
            <if test="query.createTimeEnd!= null and query.createTimeEnd!=''">
                <![CDATA[ and  create_time< date_sub(str_to_date(#{query.createTimeEnd},'%Y-%m-%d'),interval -1 day) ]]>
            </if>
        </where>
    </sql>

    <update id="updateByEmailAndCode" parameterType="com.example.mywork.entity.EmailCode">
        UPDATE email_code
        <set>
            <if test="bean.createTime != null">
                create_time = #{bean.createTime},
            </if>
            <if test="bean.status != null">
                status = #{bean.status},
            </if>
        </set>
        where email=#{email} and code=#{code}
    </update>

    <sql id="base_condition">
        <where>
            <include refid="base_condition_filed"/>
        </where>
    </sql>

    <update id="disableEmailCode">
        update email_code set status=1 where email=#{email} and status=0
    </update>

    <select id="selectByCode" resultMap="BaseResultMap">
        select
  `code`,
  `create_time`,
  `status` from email_code <where>
        code=#{code}
        </where>
    </select>

    <!-- 根据EmailAndCode删除-->
    <delete id="deleteByEmailAndCode">
        delete
        from email_code
        where email = #{email}
          and code = #{code}
    </delete>

    <!-- 根据PrimaryKey获取对象-->
    <select id="selectByEmailAndCode" resultMap="base_result_map">
        select
        <include refid="Base_Column_List"/>
        from email_code where email=#{email} and code=#{code}
    </select>

    <insert id="insertBatch" parameterType="com.example.mywork.entity.EmailCode">
        insert into email_code(
            email,
            code,
            create_time,
            status
        )values <foreach collection="list" item="item" separator=",">
        (
        #{item.email},
        #{item.code},
        #{item.createTime},
        #{item.status}
        )
    </foreach>
    </insert>

    <insert id="insert" parameterType="com.example.mywork.entity.EmailCode">
        insert into email_code(
        email,
        code,
        create_time,
        status
        )<trim prefix="values (" suffix=")" suffixOverrides=",">
        <if test="bean.email!=null">
            #{bean.email},
        </if>
        <if test="bean.code!=null">
            #{bean.code},
        </if>
        <if test="bean.createTime!=null">
            #{bean.createTime},
        </if>
        <if test="bean.status!=null">
            #{bean.status},
        </if>
    </trim>
    </insert>

    <insert id="insertOrUpdateBatch" parameterType="com.example.mywork.entity.EmailCode">
        INSERT INTO email_code(
        email,
        code,
        create_time,
        status
        )values
        <foreach collection="list" item="item" separator=",">
            (
            #{item.email},
            #{item.code},
            #{item.createTime},
            #{item.status}
            )
        </foreach>
        on DUPLICATE key update
        email = VALUES(email),
        code = VALUES(code),
        create_time = VALUES(create_time),
        status = VALUES(status)
    </insert>


</mapper>
